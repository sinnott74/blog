{
    "name": "blog",
    "version": "0.0.1",
    "description": "Blog",
    "license": "MIT",
    "private": true,
    "workspaces": {
        "packages": [
            "packages/plugins",
            "packages/client"
        ],
        "nohoist": [
            "**/*"
        ]
    },
    "scripts": {
        "start": "yarn workspace @internal/plugins build && yarn workspace client start",
        "build": "yarn workspace @internal/plugins build && yarn workspace client build"
    },
    "dependencies": {
        "typescript": "^4.2.4"
    }
}
